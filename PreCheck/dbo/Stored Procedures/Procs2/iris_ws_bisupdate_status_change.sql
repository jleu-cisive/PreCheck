CREATE PROCEDURE [dbo].[iris_ws_bisupdate_status_change]
  	-- Add the parameters for the stored procedure here
	 @crimid INT	,
	 @oldStatus VARCHAR(1),
	 @newStatus VARCHAR(1)
AS
BEGIN
	
	SET NOCOUNT ON;

   
END
